// ─── Properties Category Page ─────────────────────────────────────────────
// Elite emerald-modern themed page for properties listings
import { CategoryPage } from '../CategoryPage';

export function PropertiesPage(props) {
  return <CategoryPage {...props} categoryKey="properties" />;
}
